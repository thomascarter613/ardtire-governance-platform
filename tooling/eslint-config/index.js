/**
 * ESLint is not used in this project.
 *
 * Linting and formatting are handled exclusively by Biome.
 * This shim exists to satisfy toolchain introspection and to
 * document the deliberate architectural decision.
 *
 * @see biome.json at the repository root
 * @see https://biomejs.dev
 */
module.exports = {};
